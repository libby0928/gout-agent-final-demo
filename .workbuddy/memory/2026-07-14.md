# 2026-07-14 工作日志

## 医生端Demo单页档案优化

基于 `UX审查报告_单页档案优化.md`（11项全部执行），重构 `demos/医生端Demo.html` 的 `renderProfile` 函数。

### 核心改动

**P0 — 首屏信息完整化：**
- 完整用药方案（含合并症约束）直接放首屏，不再折叠在tab后
- 关键检验指标（血尿酸/肌酐/ALT 三行）直接在首屏展示，含化验单原件入口
- 最近2次发作记录（日期+部位+程度+处理）直接在首屏展示

**P1 — 消除冗余交互：**
- 删除 `archiveExpanded` 折叠机制 → 所有内容直接渲染
- 删除三个互斥tab（病史与发作/用药方案/检查与就诊）→ 改为连续section纵向排列
- 删除"查看完整档案"按钮 → 页面本身就是完整的
- 用药方案统一为一个完整卡片，不再重复展示
- 操作区合并为三个按钮：智能主按钮+AI辅助建议+档案维护

**P2 — 细节打磨：**
- 既往史与安全约束完整展示（含合并症用药禁忌）
- 操作按钮根据患者状态动态显示：已纳管→"复诊评估"，逾期→"联系患者"，待接诊→"开始接诊"
- 补录资料后自动滚动+绿色高亮新资料卡片（2秒动画）
- "补录资料"入口合并到"档案维护"

### 代码清理
- 删除 `setArchiveTab`、`toggleFullArchive`、`openFullArchive` 三个废弃函数
- `openPatientFlow` 移除 `archiveTab`/`archiveExpanded` 初始化
- `saveArchiveGroupForm` 移除 `archiveExpanded` 设置
- OCR补录回调改用 `_highlightNewMaterial` 标记实现自动定位+高亮
- 注解面板更新为"单页版"描述

### 修复 JS 语法错误（2026-07-14 13:12）
- `saveArchiveGroupForm` 中 toast 行的中文引号 `"医生补录"` 被JS解析器误读为变量名
- 修复：外层改用单引号包裹字符串 → `toast(item[0]+'已保存，来源"医生补录"')`
- 根因：上一轮 Edit 删除 `archiveExpanded` 行时，新旧 toast 行中文引号嵌套到了 JS 双引号字符串内部
- lint 通过，0 Error

### UX审计第二轮修复（2026-07-14 13:17）
基于自审计发现的6项问题，按优先级修复：

**N2（必须）— 智能主按钮样式失效：**
- HTML class `primary` → `primary-action`，匹配CSS选择器 `.ds-ai-brief-actions .primary-action`
- 修复后"复诊评估/联系患者/开始接诊"按钮恢复青绿渐变背景

**B1（重要）— AI临床摘要恢复三格指标卡片：**
- `aiBrief` 底部增加 `ds-ai-metrics`：血尿酸/肾功能/近期发作 三格
- CSS grid 从2列改为3列（`repeat(3,1fr)`）
- AI摘要卡片加左边框渐变色彩条（`::before` 伪元素），增强视觉重心

**B2（建议）— 既往史底部加用药安全提示：**
- `getPatientDemoData` 新增 `d.safetyTips` 字段，根据 `comorbidity` 动态生成
- 高血压患者 → "合并高血压 → 监测血压，首选氯沙坦兼具降尿酸"
- 糖尿病患者 → "合并糖尿病 → 关注肾功能，SGLT2i 可辅助降尿酸"
- 既往史 section 底部虚线分隔 + 红色标注提示行

**B4（建议）— 统一编辑入口：**
- 既往史 section 移除单独的"修改 ›"入口
- 所有 section 编辑统一走底部"档案维护"按钮，避免不一致

**B3 明确标记为二期：** AI辅助建议仍跳转独立屏，标记为 P2-4 未闭环

### 医学用语全面统一（2026-07-14 13:20）
将医生端Demo中所有面向医生展示的字段文案统一为规范临床术语：

**疾病命名：**
- "痛风"→"痛风性关节炎"（brief、AI摘要、OCR摘要中统一）
- "高血压"→"原发性高血压"（既往史字段、合并症标签、安全提示）
- "糖尿病"→"2 型糖尿病"
- "肾结石"→"泌尿系结石"

**检验指标：**
- "血尿酸"→"血清尿酸"（AI指标格、检验section、OCR识别、建档摘要、AI辅助建议屏）
- "肌酐 78 · 正常"→"Scr 78 μmol/L · 正常"
- "ALT"→"谷丙转氨酶 (ALT)"
- "肝功/肾功"→"肝功能/肾功能"（OCR关键词）

**用药用法：**
- "每日一次"→"qd"、"每日两次"→"bid"（用药方案、OCR识别、录音识别、建档摘要）
- "依从性 / 目标"→"治疗依从性 / 控制目标"
- "合并症约束"→"合并症用药注意事项"
- "忌 NSAID"→"禁用 NSAID"
- "心衰"→"心力衰竭"、"肾损者"→"肾功能不全者"

**分期/状态：**
- "长期管理期"→"痛风缓解期"
- "复诊逾期"→"复查逾期"（导航、筛选器、statusText、导出文件名、注解面板）
- "最近发作记录"→"近期急性发作记录"

**症状描述：**
- "红肿热痛"→"红、肿、热、痛"
- "疼痛"→"疼痛伴活动受限"（发作记录中区分）
- "右踝"→"右踝关节"、"踝、膝"→"踝关节、膝关节"

**字段名：**
- "发作部位"→"受累关节"、"症状状态"→"症状体征"
- "疼痛程度"→"疼痛严重程度"、"处理方式"→"治疗措施"
- "首发部位"→"首发关节"、"主要发作部位"→"主要受累关节"
- "发作记录"→"急性发作记录"（所有编辑/列表/标题/toast）
- "检查检验"→"检验记录"（分组名、上传context、OCR保存按钮、来源描述）
- "患者自填"→"患者自述"
- "坚持降尿酸治疗超过6个月"→"持续降尿酸治疗 ≥6 个月"

### 病情一句话小结（2026-07-14 13:35）

在 AI 临床摘要卡片中新增「病情小结」区域，用一句话串联患者核心用药调整史：
- 数据层：`variants` 每个患者新增 `summary` 字段，未命中 variant 的 7 名患者在 `getPatientDemoData` 中兜底映射
- 渲染层：AI 摘要卡片内，分期 chip 与风险警示之间插入 `.ds-ai-summary`（浅蓝底色 + 左侧青绿色条 + "病情小结"标签）
- CSS 新增 `.ds-ai-summary` 样式（`border-left:3px solid var(--patient-primary-2)`, `background:#f5f9fc`）
- lint 0 Error，JS 语法检查通过

### UX 信息降噪：首屏三层密度重构（2026-07-14 13:43）

按「首屏必读 / 可展开 / 底部操作」三层密度重构患者详情首屏：

**AI 摘要卡片精简：**
- 移除「当前风险与警示」长段落（风险已通过分期 chip + 警示 pill 表达）
- 警示 pill 移到三格指标下方紧贴展示
- 底部新增「查看最近检验报告 ›」链接入口

**删除重复 section：**
- 「关键检验指标」独立 section 整体删除（血清尿酸/Scr/ALT 与三格指标完全重复）

**用药方案合并为折叠卡片：**
- 长期降尿酸 + 急性发作处理合并为一张 `<details class="ds-collapse-card">`
- 标题行显示「当前用药方案」+ 摘要 badge（如「非布司他 40 mg qd + 秋水仙碱按需」）
- 展开后显示完整方案（含调整依据、启用条件、注意事项、合并症约束）

**既往史精简为一行安全摘要：**
- 首屏只显示「高血压（服药）· 药物过敏：无 · 结石史：无」
- 右侧「档案维护 ›」跳编辑入口

**发作记录首屏只保留最近 1 条：**
- 精简行格式：「近期急性发作：07-10 第一跖趾关节 · 红、肿、热、痛 · 中度」
- 超过 1 条显示「共 N 次 ›」跳全部列表

**新增 CSS：** `.ds-collapse-card` / `.ds-collapse-summary` / `.ds-collapse-body` / `.ds-safety-strip` / `.ds-ai-report-link` / `.ds-attack-strip`
- 补录资料区去chip精简：去掉标题「N 项」chip、去掉每条记录的分类 chip（病历/化验等），只保留时间+标题+描述+查看链接
- 三层信息可视化重构：
  - L1 核心层：AI 摘要三格指标改为大号数字卡片（数字在上标签在下）、用药方案从不折叠改为展开紧凑卡片紧跟 AI 摘要、主操作按钮全宽青绿渐变悬浮
  - L2 次要层：患者信息从白底卡片降级为浅灰身份条（一行：姓名+chip · 性别年龄电话，去brief）、发作/安全 strip 缩小字重和间距
  - L3 辅助层：就诊号 bar 从橙/绿色醒目条降级为浅灰、补录资料卡片缩小 padding 和字号
  - 布局节奏：区块间距改为差异化（8/14/16/20px），操作区与信息区之间大留白隔离
  - 病情小结去掉 "病情小结" label 标签
  - lint 0 Error，JS 语法检查通过

### 去重精简（2026-07-14 13:47）

- 警示从红色独立 pill 改为 AI 摘要底部一行灰色小字（`⚠ 血清尿酸未达标 · 治疗依从性差 · 缺近期肝肾功能`），去掉了视觉重量级的红色标签
- 用药 badge 从「非布司他 40 mg qd + 秋水仙碱按需」缩短为「非布司他 + 秋水仙碱」
- 用药展开后「调整依据」去掉重复的血清尿酸具体数值，改为纯策略描述
- 用药展开后合并症约束去掉「合并原发性高血压，监测血压」（安全行已有），保留通用约束
- 新增 CSS `.ds-ai-alerts-line`（灰色小字警示行）
- JS 解析通过，lint 0 Error

### 检验报告入口、按钮命名与文字精简（2026-07-14 13:56）

1. AI 摘要底部「查看最近检验报告 ›」→「检验报告（3 项）›」，点击跳 `openArchiveGroupEditor('labs')`（不再弹假报告预览）
2. 底部「档案维护」按钮 →「编辑病历」，档案编辑屏标题同步改为「编辑病历」
3. 安全行右侧重复的「档案维护 ›」入口删除，安全行变成纯只读信息条
4. 安全行只显示阳性项（高血压等），药物过敏/结石史阴性项不展示；全部阴性显示"无特殊既往史"
5. 发作记录去掉「近期急性发作：」前缀，直接日期+关节+症状；空记录显示「暂无急性发作记录」
6. 补录资料 pending 提示精简为「OCR 已识别，待确认后入档」
7. 档案编辑屏副标题精简为「选择分组后可逐条新增或编辑结构化字段」

**lint 0 Error，JS 语法检查通过**

### 用药方案模块化 + 检验报告时间轴（2026-07-14 14:02）

1. **用药方案拆为双卡片**：长期降尿酸方案（绿点+药名+剂量+用法+依从性）、急性发作处理方案（橙点+药名+剂量+用法），各自独立白底卡片，去掉调整依据/启用条件/注意事项/控制目标
2. **检验报告改为时间轴格式**：左侧竖线+青绿圆点，按报告时间倒序排列，每条显示日期+报告名称+关键指标摘要（超标红色标记）+查看原件入口
3. **数据层新增 `labReports`**：为 9 名有检验数据的患者生成 1-3 份历史报告（张明/赵海峰/孙秀兰/罗建军/吴建华各 3 份，陈建国/郭志强各 2 份，何秀英 1 份）
4. AI 摘要底部链接改为「检验报告（N 份）›」，点击滚动到时间轴 section
5. CSS 新增 `.ds-med-card`（双卡片）、`.ds-lab-timeline`/`.ds-lab-item` 等时间轴样式

**lint 0 Error，JS 语法检查通过**

---

## 创建痛风 Demo 审查团（Team 型专家）

创建了多角色 Demo 审查专家 `gout-demo-reviewer`，安装于专家中心。

### 团队构成
- **鹿通盘**（审查主编）：编排调度、交叉验证、汇编综合报告
- **林识清**（风湿免疫科临床审查员）：临床路径、病史字段、用药安全、患者安全红线
- **焦顺畅**（交互体验审查员）：点击流畅性、动线贯通、状态反馈、视觉一致性、信息去重

### SOP 三阶段
1. Phase 1（主理人）：读取 Demo HTML + 设计规范 → 打包审查上下文
2. Phase 2（并行）：spawn 两位审查员，分别按框架审查
3. Phase 3（主理人）：交叉验证 → 严重度校准 → 汇编综合报告

### 支持三种 Workflow
- 全量审查：双线并行
- 仅医学审查：跳过 UX
- 仅交互审查：跳过医学

### 文件位置
- 专家包：`~/.workbuddy/plugins/marketplaces/my-experts/plugins/gout-demo-reviewer/`
- 已注册到 marketplace，WorkBuddy 左侧专家面板可见
- 头像已生成（4 张，团队+三成员，暖橙色调统一风格）

### v1.1.0 审查团增强（14:03-14:10）
- 新增 **规范对照审计员「归合规」**（compliance-reviewer）：五步框架——41条验收标准逐条对照、18条红线审计、组件复用合规扫描、文案合规扫描、素材合规验证
- **UX审查员「焦顺畅」增强**：审查框架从4步扩展到6步。新增第三步"状态机完整性审查"（患者端10个状态逐状态检查+demoState变量覆盖+重置演示彻底性）；新增第五步"移动端适配审查"（390×844视口、键盘遮挡、附件面板弹出、横向溢出）
- **医学审查员「林识清」增强**：审查框架从3步扩展到5步。新增第四步"边缘场景与异常路径覆盖"（四类场景16个子场景：跳过中断/信息冲突/状态异常/化验异常）；新增第五步"9项字段专项审计表"；强化化验解读口径审查（AI不能给诊断结论和用药建议）
- **主理人「鹿通盘」SOP更新**：团队从3人扩展到4人；Phase 2改为三线并行（医学+UX+合规）；Phase 3综合报告增加"三线共识"交叉验证、规范合规审查发现章节；新增 Workflow 4"仅规范审查"
- plugin.json 升级至 v1.1.0，4 agents + 4 members，合规审查员头像已生成，重新注册成功

### 审查修复（2026-07-14 14:11）

根据审查报告修复 7 项问题：
- **P0**：AI 摘要底部删除冗余的「检验报告（N 份）›」链接（与下方时间轴重复，且 labReports 为空时点击无反馈）
- **P1**：时间轴「查看原件」传入日期+报告名称参数，不同报告展示不同弹窗内容
- **P1**：安全行增加家族史展示（"家族史不详"），不再丢弃该字段
- **P1**：补录资料区 box-shadow 已在前序迭代中去掉（确认无需改动）
- **P2**：用药方案 `medPlan` 按患者实际 `med` 字段动态生成（苯溴马隆/别嘌醇/非布司他），不再写死
- JS 语法检查通过，lint 0 Error

### 视觉容器统一（2026-07-14 14:18）

从 8 种视觉容器统一为 3 种：
- **A 型（核心卡片）**：AI 摘要（渐变+阴影+左侧色条）、用药方案双卡片（白底+边框，去阴影）
- **B 型（信息条）**：患者身份、发作记录、安全摘要——全部改为无背景无边框纯文字行
- **C 型（辅助区）**：就诊号（浅灰底小圆角）、补录资料 section（浅灰背景 `#f8fafb`）
- 补录资料内部卡片保持白底以区分层级
- 用药方案卡片去 `box-shadow`，与 AI 摘要同视觉节奏
- JS 语法检查通过，lint 0 Error

### v1.2.0 审查团增加视觉设计审查员（14:17）

- 新增 **视觉设计审查员「颜颜值」**（visual-reviewer）：五步框架——
  1. 色彩 token 落地审计（患者端9色+医生端11色逐色对照 tokens.css，检查硬编码、色板超标、透明度不一致）
  2. 排版字体层级审计（font-scale 8档逐字号检查、字重一致性、行高统一、字体族）
  3. 间距圆角体系审计（4px栅格7档间距+7档圆角，检查magic number和同类组件圆角不一致）
  4. 阴影图标装饰审计（4档预设阴影、31个ui-icon图标一致性、emoji/单字违规、装饰元素）
  5. 动画过渡与品牌调性（页面切换/弹窗/按钮/对话追加四类动画审计 + 视觉重心可读性 + 患者端轻盈感/医生端任务优先调性连贯性）
- 输出：结构化表格 + 六大输出项（含色彩token覆盖率报告、排版一致性报告、间距圆角合规率报告、图标合规报告、动画缺失清单、美观度评分1-10）
- **主理人 SOP 更新**：团队从4人扩展到5人；Phase 2 改为四线并行（医学+UX+视觉+合规）；Phase 3 综合报告新增"视觉设计审查发现"章节；新增 Workflow 4"仅视觉审查"
- plugin.json 升级至 v1.2.0，5 agents + 5 members，视觉审查员头像已生成，重新注册成功
- 审查团现覆盖 5 个维度：临床医学 + 交互体验 + 视觉设计 + 规范合规 + 主理人交叉汇编

### v1.3.0 视觉审查员新增页面布局建议能力（14:20）

- **颜颜值审查框架从5步扩展到6步**：新增第六步「页面布局优化建议」——
  - 审查与建议双重角色：先审问题再给方案
  - 四维分析框架：信息层级重组、空间利用率、模块编排顺序、视觉浏览动线（F型/Z型/引导型/焦点型）
  - 建议输出格式：每页含当前布局问题+优化前后结构示意+关键改动说明+预期效果
  - 六条原则：基于现有Demo不凭空造轮子、具体到模块名、给出理由不只结论、可落地（DOM顺序/折叠/合并卡片）、患者端和医生端分开、只改布局不越界
  - 患者端5个审查重点：状态卡视觉权重、对话与任务卡关系、底部四胶囊与内容区去重、病历夹信息密度、联系医生页布局
  - 医生端5个审查重点：工作台信息密度与效率、患者队列列表密度、详情档案模块编排、操作入口可达性、就诊码视觉优先级
- 输出规范增加第七大输出项「页面布局优化建议报告」
- 主理人 SOP 更新：Phase 2 颜颜值任务描述更新为六步框架；Phase 3 综合报告新增「六、页面布局优化建议」章节，后续章节编号顺延
- plugin.json 升级至 v1.3.0，颜颜值 profession 更新为「视觉设计与布局审查员」

### 板块色块区分 + 检验报告折叠（2026-07-14 14:20）

1. **板块色块区分**：每个板块用不同浅色背景区分——
   - 用药方案：浅绿底色 `rgba(3,212,159,.06)` + 绿色边框
   - 急性发作：浅橙底色 `rgba(237,123,47,.06)` + 橙色边框
   - 检验报告：浅蓝底色 `rgba(1,161,255,.05)` + 蓝色边框
   - 既往史与安全：浅灰底色 `rgba(86,101,121,.05)` + 灰色边框
   - 补录资料：浅灰蓝底色 `rgba(31,42,68,.03)`
   - 每个板块加 `.ds-section-label` 小标题（灰色小字标识）
2. **检验报告折叠**：默认只展示最新一份报告，其余用「查看历史报告（N 份）」折叠按钮收起，点击展开/收起带箭头旋转动画
3. 新增 CSS：`.ds-section-block`、`.ds-section-label`、`.ds-lab-fold`、`.ds-lab-fold-body`
4. 新增 JS 函数：`toggleLabFold()`
5. JS 语法检查通过，lint 0 Error

### 用药板块编辑+拍药盒按钮（2026-07-14 14:28）

1. 用药方案板块标签行右侧新增两个小按钮：「编辑」+「拍药盒」
2. 「编辑」→ 跳转 `openArchiveGroupEditor('medication')` 进入结构化编辑
3. 「拍药盒」→ 调用 `openOcrCamera('medication', ...)` 打开 OCR 相机，标题显示"拍摄药盒"
4. OCR 确认后自动归库为"用药记录"，演示数据：非布司他 40 mg · qd · 待医生确认
5. 新增 CSS：`.ds-section-actions`、`.ds-section-btn`（圆角小按钮，蓝色边框+文字）
6. `.ds-section-label` 改为 flex 两端对齐以容纳按钮
7. JS 语法检查通过，lint 0 Error

### 发作板块新增发作按钮（2026-07-14 14:29）

1. 近期急性发作板块标签行右侧新增「新增发作」按钮（复用 ds-section-btn 样式）
2. 点击跳转 `openArchiveGroupEditor('attack')` 进入发作记录结构化编辑
3. 无论当前有无发作记录均显示按钮
4. JS 语法检查通过，lint 0 Error

### 既往史编辑入口+仅展示阳性项（2026-07-14 14:30）

1. 既往史与安全板块标签行右侧新增「编辑」按钮，跳转 `openArchiveGroupEditor('safety')`
2. 过滤逻辑改为 `item.status !== '无' && item.status !== '不详'`，只展示阳性项
3. 原本"原发性高血压"无论有无都展示，改为仅 status 非无/不详时展示
4. 全部阴性或无信息时显示"无特殊既往史"
5. JS 语法检查通过

### 全局标注面板（2026-07-14 14:31）

1. 右上角新增固定「注」按钮，点击打开标注面板+半透明遮罩
2. 面板顶部可切换查看不同页面的标注（横向滚动 chips）
3. 已覆盖 20 个 screen 的标注数据：登录/工作台/患者列表/详情档案/编辑病历/补录资料/AI辅助/确认接诊/消息/聊天/设置/二维码/建档/逾期/语音问诊/语音确认/就诊扫描/OCR相机/就诊码
4. 每个页面按板块分组（section），每项标注包含：元素名、显示条件、数据来源
5. 标注内容放在 `ANNOTATIONS` 对象中，前后端可按需补充
6. 面板默认隐藏，点击按钮或遮罩关闭
7. JS 语法检查通过
