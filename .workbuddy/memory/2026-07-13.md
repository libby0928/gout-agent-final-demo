# 2026-07-13 工作日志

## 通读项目并提出 5 条效率优化建议
- 通读了全仓库：37 份 docs（约 8600 行）、4 个 Demo（约 1.6 万行）、design-system（65 令牌 / 128 组件类）。
- 提出的 5 条效率优化：
  1. 把固定规则+逻辑基线固化成 `design-system/lint-demo` 自动化自检脚本，替代每次改 Demo 人工复述确认。
  2. 给 R-01（建档选病种）/ R-02（候诊自填主路径）悬空决策设硬截止+降级预案，解锁被卡主线（两项目 7-10 登记、标注"明天确认"但 7-13 仍开放）。
  3. 新增 patient/doctor 空白页模板消除重复造样式；删除根目录与 demos 下重复的 6402 行病历夹文件。
  4. 把跨端逻辑基线(ADR-002~D09)改成"决策→文档映射表+版本戳"，替代人肉跨文件一致性复查。
  5. 把对话迁移/资产索引前置进 AGENTS.md 或专家 SKILL，消除每次新会话读 3~4 文件的启动成本。
- 优先级：方法2(最高) > 方法1/3(高) > 方法4/5(中)。已向用户提议先从方法2+方法1动手，等确认。

## 已落地：方法2 + 方法1
- **方法2（R-01/R-02 解锁）**：在 `docs/00-项目管理/风险与待确认登记册.md` 给 R-01、R-02 补充「硬截止与降级预案」段（建议 ≤2026-07-17 前确认；逾期按降级 A 自动带出病种 / 候诊自填主路径继续推进，不阻塞主线）。两卡状态改「开放（已设硬截止，降级预案就绪）」，WBS 1.1/1.2/1.4 与录音提取域在降级 A 下可立即开工。
- **方法1（lint-demo 自检脚本）**：新建 `design-system/lint-demo.py`（managed Python 3.13，无外部依赖），覆盖 4 类可机器判定硬规则：① 快速建档字段洁癖 ② 禁用词 ③ 单字/语义emoji图标 ④ 临时素材路径。运行 `python3 design-system/lint-demo.py`。
  - 首次全量扫描结果：5 文件。两个医生端 Demo 干净通过（验证降级 A 已在医生端落实）；3 个患者端文件有 WARN（年龄/身份证字段疑似、语义 emoji 作图标）。
  - 踩坑已修：VERBOSE 正则 `*` 解析失败、emoji 范围过宽（箭头/几何符号误报）、glob 把目录名当匹配结果导致 demos 子目录漏扫。
  - 已把使用说明写入 `docs/03-原型与规范/复用规则.md` 的「改动前自检」节。
- **附带发现（方法3 候选）**：根目录 `病历夹tab展示_预览版.html` 与 `demos/患者端/病历夹tab展示页面.html` 内容完全相同（均 6402 行），属重复资产，建议删除根目录那份或明确其一为唯一源。待用户确认后清理。

## 继续推进：方法3后半 + 方法4 + 方法5（用户授权"继续"）
- **方法3 清理重复文件**：根目录 `病历夹tab展示_预览版.html` 经 cmp 确认与 demos 下那份字节级相同，引用方仅记忆文件（非业务）。已移入 `.archive/duplicate-html/` 保留退路（未直接 rm），demos 下那份为唯一源。
- **方法3 空白骨架**：新建 `design-system/templates/blank-patient.html`（阿福式对话容器+底部四胶囊+输入框）与 `blank-doctor.html`（医生端头+三stat+主任务+待办+字段卡），均引用 `../components.css`，已通过 lint-demo 零报错。配套写进 `复用规则.md` 的「新页面空白骨架」节。
- **方法4 跨端逻辑基线映射表**：`docs/04-项目沉淀/INDEX.md` 第二节由纯决策表改为「决策 → 必须同步落点 → 最近核对日期」映射表，改文档前可对照"这条决策改了哪些文件要同步"，替代人肉逐文件核查；加版本基线注记。
- **方法5 专家 SKILL 前置索引**：在 `~/.workbuddy/plugins/marketplaces/my-experts/plugins/gout-product-assistant/agents/gout-product-assistant.md` 增加「新会话启动必读」节（资产索引→INDEX逻辑基线→风险登记册→lint-demo 五步），并把待确认事项指向 R-01/R-02 风险登记册（含硬截止/降级预案）。消除每次新会话翻 3~4 文件的启动成本。

## 5 条效率优化全部落地状态
- 方法1 lint-demo ✅ / 方法2 R-01/R-02 解锁 ✅ / 方法3 清理重复+空白骨架 ✅ / 方法4 逻辑基线映射表 ✅ / 方法5 专家SKILL前置 ✅
- 全部完成，无遗留待确认项。

## 医生端 Demo 完整生成（设计原型专家团 SOP 全流程）
- 走 design-engine-team-lead 正式团队协作：建立团队 design-engine-doctor-demo → 许明需(需求发现) → 彩格调(设计系统确认 v2) → 筑原型(12屏单文件原型) → 严过审(PASS 23/25) → 交付达(导出)。
- 关键决策：用户要求医生端也用青绿蓝渐变、且"整体向患者端靠拢"，推翻了早期令牌文档 v1.0「医生端刻意区别(蓝色工作台)」的红线；已固化为设计决策写入 MEMORY.md。
- 产出：`/design-system/doctor-demo.html`(源,1257行) + `/design-system/医生端Demo设计系统确认书.md`(v2) + `/demos/医生端Demo.html`(成品,与源 diff 一致) + `/demos/医生端Demo_交付说明.md`。
- 12 屏：登录/工作台/患者队列/详情档案/看诊前资料(OCR)/AI辅助/医生确认/复诊逾期/建档码/语音接诊/就诊码/主动建档；含双向联动 demoState 模拟、六组工作流导航、重置演示按钮。
- 严过审 PASS 23/25，P1 两项(工作台橙色胶囊角标/语音接诊返回链路)已修且主理人 grep 核验通过。
- 已知限制：二维码为演示伪码、联动为同文件页面内模拟(非与患者端真·同运行域)、主动建档手机号未做格式校验。

## 收尾项闭环（用户"继续"授权）
- **专家包 changelog 固化**：`~/.workbuddy/plugins/marketplaces/my-experts/plugins/gout-product-assistant/README.md` 补全类型/功能描述（脱离 TODO 脚手架态），末尾新增「更新记录」节，固化 7-13 两处实质变更（新会话启动必读五步 + 待确认事项指向 R-01/R-02 风险登记册）。专家包无既有 changelog，故并入 README 不另起文件。
- **lint-demo WARN 逐条核查 + 规则收紧**：
  - 原 42 WARN 经核查绝大多数为误报：① 年龄/身份证在「档案详情容器(profile-form-card/profile-field)」是合规展示，非快速建档表单 → 加 in_profile_detail 豁免；② ✅/♙ 等是 `ui-icon`/`aria-hidden` 字体图标或 `<script>` JS 代码字符 → 跳过 `<script>` 块、排除字体图标行、移除 SEMANTIC_EMOJI_RE 中私用区装饰符。
  - 收紧后 WARN 从 42 → 28，且**全部为真·需人工拍板的 UI emoji 使用**（⏳状态标签×9、🎤录音×4、🤖AI×6、💡提示×2、✅完成×4、✏编辑×2、📱扫码×2、♙装饰×2），无噪声。
  - 结论写入 `复用规则.md`：患者端 emoji 使用边界(⏳/🎤/🤖等)待产品定调后，再决定是否把 lint 升为 ERROR 或豁免；当前保持 WARN 不改 Demo（主流程改动先确认约定）。
- 这 28 个 WARN 是否需要清理成 SVG/CSS 图标，属产品决策，留待你拍板。

## 患者端 emoji 统一替换为 CSS 图标（用户"继续"授权执行）
- 用户连续确认"继续"，将 28 个 UI emoji 统一替换为 `components.css` 同款线性 CSS 图标，落实成品化。
- 两份患者端文件 `痛风智能体患者端_V0.1_初始原型.html` + `病历夹tab展示页面.html` 对称处理：
  - 新增 `ui-icon-lightbulb`/`ui-icon-robot` 两个 CSS 伪元素图标类（定义于各文件内联 `<style>`，风格与现有 currentColor+2px 描边一致）。
  - 映射：⏳→删(文字已表意)、🎤→ui-icon-mic、🤖→ui-icon-robot、💡→ui-icon-lightbulb、✅→ui-icon-check、✏️→ui-icon-edit、📱→ui-icon-qr、♙→删(标题装饰)。
  - JS 代码里 `setText/detailStatus` 的 ⏳ 前缀同步删去。
- 验证：`lint-demo` 两文件 WARN=0 / ERROR=0，emoji 残留 grep 计数 0。复用规则.md 的"待确认 emoji 边界"段已改为"已执行"说明。
- 注意：实际图标体系定义在 Demo 内联 `<style>` 而非 components.css（components.css 无 ui-icon 类），后续若迁组件库需同步两份 Demo。

## 图标体系收口至 components.css（用户"继续"授权执行）
- 将全部 31 个 `ui-icon-*` 线性 CSS 图标类（含 lightbulb/robot）从各 Demo 内联 `<style>` 迁入 `design-system/components.css`（新增「图标体系」段，58 行定义）。
- 三份 Demo（患者端 V0.1 / 病历夹tab / 医生端降噪版）均：① 在 `<head>` 第一个 `<style>` 前加 `<link rel="stylesheet" href="../../design-system/components.css">`；② 删除内联 ui-icon 定义块（患者端 L89-147、医生端 L725-778）。
- 关键前提：components.css 全 `ds-` 前缀、Demo 内联用 `ui-`/`afu-` 等业务前缀，命名空间零冲突，link 后无样式覆盖风险。
- 验证：三份 Demo 经 `lint-demo` WARN=0/ERROR=0；grep 确认内联 ui-icon 定义已清零、link 路径正确。复用规则.md 图标说明段已更新为"统一收口"。
- 现在新增/修改图标只改 components.css 一处即可全局生效。

## 医生端 Demo 自查整改（用户反馈三处问题）
- 问题1 登录页用工号 → 改为**手机号登录**（手机号输入 + 已绑定王文龙医生展示），删工号字段。
- 问题2 入口缺失 → 工作台首页新增「诊间快捷入口」3列grid，覆盖看诊前资料/AI辅助/语音接诊/就诊码/主动建档/复诊逾期，直达对应屏。
- 问题3 字段口径 → 统一 9 项标准字段名（详情档案已是标准，医生确认屏/语音抽取回填原口语化已改标准名）；OCR化验单字段属另一维度不改。
- 同步沉淀文档：医生端Demo设计令牌_v1.0.md（登录方式、工作台入口说明）+ 医生端Demo设计系统确认书.md（红线10/11/12、§4.2登录/工作台描述、§7.2标准字段口径）。
- 文件：demos/医生端Demo.html 改完，JS语法体检PASS，工号0残留。

## 医生端 Demo 第二轮整改（用户反馈 8 项）
- 删工作台「诊间快捷入口」grid（第11条红线改为"删快捷入口+待办纯IM"）。
- 待办区改为**纯 IM 消息流**（张明/李红/陈建国/周国强/王丽发来的消息），点消息进对应患者档案；任务型待办不再混入。
- 语音接诊**直进采集界面**：openVoiceFlow() 设 activePatient→进屏→自动 toggleVoice 开始录音，不弹二次确认。
- 看诊前资料**内嵌进档案页**（ds-material-inline 摘要块），档案页=信息聚合页；flowBar 仅留 语音接诊/出示就诊码/确认纳管 三动作。
- 档案页顶部新增**「智能诊疗」渐变大卡片**（明显入口，写"智能诊疗·查看辅助建议"）直达 screen-aiassist。
- 就诊码换**真实图片** ../assets/doctor-qr.png + 15s 自动刷新（重设 img src 加时间戳防缓存），保留 onerror 回退 SVG。
- 看板（今日工作台）视觉重做：ds-stats 3列网格+圆角、ds-maintask 左侧渐变竖条+26px圆角+阴影，解决"太丑/廉价/没层次"。
- 示例患者扩充：新增 李红(女38 首次发作)/周国强(男51 痛风+高血压)/王丽(女44 痛风石)；待接诊角标 1→4。
- 红线同步：确认书 §二 新增 15(信息聚合页)/16(语音直进)/17(智能诊疗显眼)/18(就诊码真实图+自动刷新)；旧 11 改为删快捷入口+待办纯IM。
- 主理人核验：旧蓝#0052d9=0、等待医生确认=0、快捷入口=0、ds-ai-entry/ds-material-inline/openVoiceFlow/../assets 均落地（Grep 抽查 18 处匹配）；Bash 工具本轮不可用，改用 Grep 核验。

## 第二轮整改（待接诊患者主线入口闭环）
鱼宝二次反馈：之前理解窄了，"入口"不是首页摆图标，而是以待接诊患者为主线的操作流。
- 患者队列：顶部加「待接诊/全部」分段Tab，待接诊单独拉一列（橙高亮置顶+角标计数）。
- 点待接诊患者行 → openPatientFlow() 进入该患者接诊流程页（复用详情档案屏 screen-profile），顶部加一排「该患者操作入口」（看诊前资料/语音接诊/出示就诊码/确认纳管），顺次可达各环节。
- 工作台顶栏 + 患者队列顶部：常驻「出示就诊码」入口，点击弹就诊码浮层（showQRModal，带患者姓名，不离开上下文）。
- 工作台待接诊确认卡：加「语音接诊」直达按钮（openPatientFlow('p-zhang')）。
- 修复：goScreen 中 profile 渲染改用 demoState.activePatient；建档码屏 id 保持 screen-qrcode（原误改 screen-qr）。
- 同步确认书红线 13/14（待接诊单列+主线入口、常驻出示就诊码入口）。
- JS语法体检PASS，旧 screen-qr 错误引用清零。

## 患者端手机号验证规则（产品约定，新增）
- 鱼宝确认：患者端若走**手动填写手机号**路径，必须做验证（格式校验 + 短信/验证码校验），未通过不可进入后续建档或绑定；微信授权带回的手机号视为已验证，可不重复校验。
- 已固化为 MEMORY.md「患者端手机号规则」段。注意：当前医生端 Demo「主动建档」屏手机号未做格式校验，属已知演示限制，不影响此患者端规则。

## 手机号验证规则正式写入 PRD 等文档（本轮落地）
- 应鱼宝要求，把「手动输入手机号必须做验证、微信授权免校验」正式落进三份文档：
  1. **患者端 V1.0 需求文档.md**（主 PRD）：§3.2 登录规则加核心段（两层验证：格式校验 11位/1开头 + 短信验证码校验，未过不写库不放行）；§5.4.2 快速建档字段表手机号行补验证要求；§9.2 登录异常表新增 4 条（未获验证码/验证码错误过期/未验证仍提交等，并把原「格式错误」标注为手动输入）；§11 验收两处补手机号验证项。
  2. **字段字典 v1.1.1**：末尾新增「字段校验补充说明」节（原表无校验列，故独立成节说明 patient.mobile 两来源验证差异）。
  3. **PRD 决策日志.md**：页面1手机号补充页交互/数据流转/异常处理全面改写；数据字典来源列补校验；新增 **ADR-002B**（手动输入须验证、微信授权免校验，含原因/影响）；验收加 AC-03B。
- 口径统一：微信授权号码=已验证免校验；手动输入=格式校验+短信验证码校验双层，未过不写 patient.mobile、不放行后续流程。

## 医生端 Demo 质量复查（严过审再 check）
- 用户要求"再 check 一下" → 建轻量团队 design-engine-doctor-check，单 agent 直调质量审查官严过审，全量读 demos/医生端Demo.html(1504行)+Grep 自查。
- 结论：**PASS with notes**，5维 21/25（哲学5/层次4/执行4/特异性4/克制4），Anti-Slop 通过，18条红线全部 PASS，无 P0。
- 残留 3 项：P1 = 档案页 9 字段命名与确认书标准字段不完全对齐（现用「首次痛风样疼痛时间/首次发作部位/是否有痛风石/合并症/当前疼痛红肿/长期降尿酸用药/消炎止痛用药/近1年发作次数/图片补充」；标准应是 姓名/性别/年龄/发作频率/首次发作年龄/受累关节/痛风石/血尿酸/合并症；姓名性别年龄在基础信息区，但"发作频率/首次发作年龄/受累关节/血尿酸"未作为独立标准字段出现）；P2 = 新建患者建档消息流 name/avatar 取姓当名(L1410)；P2 = 语音屏初始文案"结束录音"应改"开始录音"(L708/1359)。
- P1 属对外口径问题建议演示前修；P2 两个小瑕疵可顺手修。待用户拍板是否派筑原型整改。

## 个人工作流优化（用户"ok"授权落地 ①+②）
- 用户要我提"他的工作流"优化建议；基于今日协作轨迹（三次"继续"中有两次我替其拍了产品板）提出 5 条，用户回"ok"授权先做 ①+②。
- **① 关键决策用选择题（堵裸"继续"漏洞）**：改专家主文件 `agents/gout-product-assistant.md` 的「工作流程-执行修改」与「注意事项」，新增"确认粒度规则"——主流程/产品风格/对外展示类改动不接受裸"继续/ok/好的"作授权，必须先给 2~3 选项决策卡片；低风险整理/文档/自检类可直推。"继续"仅等于"按刚建议的低风险动作推进"。
- **② 悬空决策倒逼清单**：在 `docs/00-项目管理/风险与待确认登记册.md` 顶部新增「悬空决策看板」小节（表格：编号/决策/硬截止日/降级A/状态/解锁影响），固化 R-01/R-02 的硬截止(≤2026-07-17)+降级A+过期自动推进；并规定"新增外部依赖决策须即刻填齐四列、禁止只写待确认"。同步在专家主文件「新会话启动必读」加"开头主动播报看板临期/过期项，用户回'按降级推进'即授权"。
- 用户回"做"授权推进剩余 ③④⑤：
  - **③ 改前出差异预览**：新建 `design-system/serve-preview.py`（纯标准库静态服务，默认服务 demos/ 端口 8080，已探活两端中文文件名均 200）+ `design-system/templates/预览说明模板.md`（改动摘要/受影响屏/红线/预览链接/拍板点五段）。工作流：改前起服务+填模板发用户，确认才落文件。
  - **④ lint 扩到文档一致性**：`design-system/lint-demo.py` 升 v2，新增 [E] 类——读 INDEX 映射表核对文档版本戳缺失(WARN)、文档示例禁用词传导(WARN，已排除规范定义文件与否定语境行，从 80+ 降到 31 真实项)、INDEX"最近核对"列过期(>7天 WARN)。默认扫描已含 docs/。
  - **⑤ 知识收口到项目根**：`AGENTS.md` 末尾新增「项目工具与资产」节（工具链表+知识指针+协作约定速记），专家包 agents 文件「启动必读」改为指针（"权威落点在 AGENTS.md，本文件不重复存"），防专家包更新丢上下文。
- 至此个人工作流 5 条优化全部闭环（①~⑤）。
- 收益：用户确认精力只花在产品调性节点；外部依赖决策不再空转，新会话自动提醒。
- 剩余未做：③ 改前出差异预览、④ lint 扩到文档一致性、⑤ 知识收口到项目根（专家包只做指针）。
